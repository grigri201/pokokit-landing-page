# Decision Log: Pokopia Ecosystem Landing Page Brief

## 2026-05-18

- 创建模式：`bmad-product-brief -A`，按 headless create 执行。
- 文档语言：中文。
- 输出位置：`_bmad-output/planning-artifacts/briefs/brief-landing-page-2026-05-18/`。
- 产品定位：统一整合目录 / ecosystem landing page，不是合并两个项目代码，也不是替代各项目 PRD。
- 第一批项目：`../pokopia-color-pattern` 的 Pokopia Decor Dex；`../pokopia-scene-editor` 的 Pokopia 5×5/7×7 Scene Editor。
- 数据策略：第一版采用 landing-page 仓库人工维护 project manifest；输入项目文档仅用于初始化文案和边界。
- 数据边界：不默认读取或复制各项目静态数据、构建产物、raw assets、SceneDocument payload 或 build-only diagnostics。
- 未来扩展：如需自动同步项目状态，由项目提供小型公开 `project.manifest.json`，landing page 只读取项目级公开元数据。
- Decor Dex 特别边界：不重新引入 `docs/pokopia_image_sources/**` 或 `dist/docs/pokopia_image_sources/**`，尊重其 runtime asset boundary。
- Scene Editor 特别边界：`SceneDocument v1` 是编辑器保存/恢复契约，不是目录页数据源；`selectedPokemonKey` 使用 Decor Dex key space 只表示语义关联。
- 外链策略：区分打开工具、查看详情、查看仓库、查看规划文档；不可用入口显示状态说明。
- 未确认：Scene Editor 公开部署 URL、landing page 正式域名、未来项目 manifest 是否强制。
