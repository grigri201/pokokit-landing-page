# Story 1.3: 支持状态和能力标签筛选

Status: done

## Story

As a Pokopia 创作者,
I want 按项目状态和能力标签筛选 Project Card,
so that 我可以快速找到当前任务需要的工具。

## Acceptance Criteria

1. Given 用户在 Home Page 选择 `available` 状态筛选, when 项目列表更新, then 页面只显示状态为 `available` 的项目, and 清除筛选后恢复完整项目列表。
2. Given 用户选择 Capability Tag `建筑层`, when 筛选生效, then 列表定位到 Scene Editor, and 用户选择 `装饰推荐` 时列表定位到 Decor Dex。
3. Given 用户同时选择 status 和 capability filters, when 多个筛选条件生效, then 列表只显示满足全部条件的项目, and filter state 反映在 URL search params 中，而不是 `localStorage`。
4. Given 筛选结果为空, when 列表没有匹配项目, then 页面显示空结果标题、原因和清除筛选操作, and 不显示空白页面。
5. Given 用户只使用键盘或屏幕阅读器, when 操作筛选控件, then filter buttons 或 checkbox 语义清楚，selected state 可通过文字/ARIA 理解, and focus state 可见。

## Tasks / Subtasks

- [x] Task 1: 添加筛选 domain/lib 逻辑 (AC: 1, 2, 3, 4)
  - [x] 实现 status/capability filter parsing、URL search params serialization 和 AND 过滤。
  - [x] 添加空结果检测和 clear filter helper。
- [x] Task 2: 实现可交互 FilterToolbar 与 EmptyState (AC: 1, 2, 3, 4, 5)
  - [x] 将 Story 1.2 的 disabled preview 替换为可操作 button。
  - [x] 使用 `aria-pressed` 和可见 selected state 表达状态。
  - [x] 添加清除筛选操作和空结果状态。
- [x] Task 3: 添加测试并验证 (AC: 1, 2, 3, 4, 5)
  - [x] 覆盖 helper tests：status、capability、AND、search params。
  - [x] 覆盖 HomeRoute 交互 tests：筛选、URL 更新、空状态、清除。
  - [x] 运行 `pnpm typecheck`、`pnpm test`、`pnpm lint`、`pnpm build`、`pnpm smoke`。

### Review Findings

- [x] [Review][Patch] 浏览器 Back/Forward 必须同步 URL search params 和可见筛选状态。
- [x] [Review][Patch] 状态筛选选项必须从 manifest / label maps 派生，不能硬编码。
- [x] [Review][Patch] `全部项目` 当前态必须有可访问 selected state。
- [x] [Review][Patch] 无效 capability query param 必须忽略，避免不可见 active filter。

## Dev Notes

- Filter state 在 MVP 中必须使用 URL search params，例如 `?status=available` 和 `?capability=建筑层`；不得用 `localStorage`。[Source: `_bmad-output/planning-artifacts/architecture.md#frontend-architecture`]
- 多筛选条件按 AND 逻辑组合，空结果必须显示说明和清除筛选操作。[Source: `_bmad-output/planning-artifacts/epics.md#story-13-支持状态和能力标签筛选`]
- Filter controls 使用 button 或 checkbox 语义，并通过 text/ARIA 表达 selected state。[Source: `_bmad-output/planning-artifacts/ux-design-specification.md#search-and-filtering-patterns`]

### Project Structure Notes

- 复用 `src/lib/filter-options.ts` 和 `src/domain/project-labels.ts`，不要在组件中复制 filter option 列表。
- Story 1.4 会继续扩展 Entrypoint 视觉/语义；本 story 只处理筛选。

### References

- `_bmad-output/planning-artifacts/epics.md#story-13-支持状态和能力标签筛选`
- `_bmad-output/planning-artifacts/architecture.md#filter-patterns`
- `_bmad-output/planning-artifacts/ux-design-specification.md#search-and-filtering-patterns`

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `pnpm typecheck`
- `pnpm test`
- `pnpm lint`
- `pnpm build`
- `pnpm smoke`

### Completion Notes List

- 新增 `src/lib/filters.ts`，支持 status/capability URL search params、toggle、AND 过滤和 active filter detection。
- 新增可交互 `FilterToolbar`，通过 `aria-pressed` 表达 selected state，筛选状态写入 URL 而不是 localStorage。
- 新增 `EmptyState`，在筛选无结果时展示原因和清除筛选操作。
- 添加 helper 和 HomeRoute 交互测试，覆盖 status、capability、AND、空状态、清除筛选和 URL 更新。
- 根据 code review 补充 popstate 同步、manifest-derived status filter options、`全部项目` selected state 和无效 query 忽略。

### File List

- `_bmad-output/implementation-artifacts/1-3-支持状态和能力标签筛选.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `src/components/EmptyState.tsx`
- `src/components/FilterToolbar.tsx`
- `src/lib/filters.test.ts`
- `src/lib/filters.ts`
- `src/lib/filter-options.ts`
- `src/routes/HomeRoute.test.tsx`
- `src/routes/HomeRoute.tsx`
- `src/styles/layout.css`

### Change Log

- 2026-05-19: Implemented status/capability URL filters, AND filtering, empty state, and clear action.
- 2026-05-19: Addressed code review findings for popstate sync, manifest-derived status filters, selected state, and invalid query handling.
