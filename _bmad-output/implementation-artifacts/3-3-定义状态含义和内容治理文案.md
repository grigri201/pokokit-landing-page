# Story 3.3: 定义状态含义和内容治理文案

Status: done

## Story

As a Pokopia 创作者或维护者,
I want 明确理解每个项目状态和内容新鲜度的含义,
so that 我不会把人工维护的目录状态误解成实时监控或已上线承诺。

## Acceptance Criteria

1. Given status label maps 定义项目状态, when 页面展示 `planned`、`in-development`、`available`、`experimental`、`maintenance` 或 `archived`, then 每个状态都有用户可读含义, and `in-development` 不暗示工具可启动。
2. Given Project Card 或详情页展示人工维护状态, when 用户阅读状态和 `dataFreshness` 文案, then 页面不把手动状态描述成 real-time、live、自动同步或监控数据, and 维护者能区分 `manual`、`build-time`、`project-manifest` 和 `unknown`。
3. Given Entrypoint 或项目状态不可用, when 页面展示状态说明, then 说明靠近相关入口或状态徽标, and 用户能理解可执行下一步，例如查看详情、查看规划文档或等待公开 URL。
4. Given 维护者需要补充短备注, when manifest 包含 maintainer notes, then notes 用于部署未知、边界提醒或维护说明, and 长实现理由仍留在 architecture、PRD 或 addendum 中。

## Tasks / Subtasks

- [x] Task 1: 固化状态和新鲜度文案治理 (AC: 1, 2)
  - [x] 覆盖 `projectStatusLabels` 的所有 `ProjectStatus` enum，确认 label/description 非空且 `in-development` 不暗示可启动。
  - [x] 覆盖 `dataFreshnessLabels` 的所有 `DataFreshness` enum，确认 `manual` 文案明确人工维护且不表达实时、live、自动同步或监控。
- [x] Task 2: 验证不可用入口的下一步说明 (AC: 3)
  - [x] 为 `disabled`、`local-only`、`tbd` entrypoint availability 增加可读说明断言。
  - [x] 确认 Scene Editor 的未部署 tool entrypoint 靠近入口展示原因，并仍提供详情、规划文档或本地仓库等下一步。
- [x] Task 3: 验证 maintainer notes 内容边界 (AC: 4)
  - [x] 测试 notes 保持短备注用途，不包含长实现理由或替代 PRD/architecture 的叙述。
  - [x] 确认现有 notes 只用于部署未知、边界提醒或维护说明。
- [x] Task 4: 运行质量门禁并更新记录 (AC: 1-4)
  - [x] 运行 `pnpm typecheck`、`pnpm test`、`pnpm lint`、`pnpm build`、`pnpm smoke`。
  - [x] 更新 Dev Agent Record、File List、Change Log，并将状态推进到 review。

## Dev Notes

- Status、availability、type、dataFreshness label maps 必须集中在 domain 层复用；Project Card、Detail Page、filters 和 tests 不得各自复制状态文案。[Source: `_bmad-output/planning-artifacts/epics.md#additional-requirements`]
- FR-25 要求 `planned`、`in-development`、`available`、`experimental`、`maintenance`、`archived` 都有用户可读含义；`in-development` 不能暗示工具可启动。[Source: `_bmad-output/planning-artifacts/prds/prd-landing-page-2026-05-18/prd.md#fr-25-状态含义文案`]
- FR-26 要求 `manual`、`build-time`、`project-manifest`、`unknown` 的内容新鲜度可区分，manual 不得描述为实时。[Source: `_bmad-output/planning-artifacts/prds/prd-landing-page-2026-05-18/prd.md#fr-26-内容新鲜度`]
- FR-28 允许短 maintainer notes，但 notes 不能替代项目 PRD；长实现理由必须留在 architecture、PRD 或 addendum。[Source: `_bmad-output/planning-artifacts/epics.md#story-33-定义状态含义和内容治理文案`]
- Story 3.2 已增加 source/script/dist 数据边界门禁；本 story 不应放宽 `pnpm build` 的边界测试，也不应引入实时状态或 link checking。
- 当前实现中 `src/domain/project-labels.ts` 已集中定义 status、entrypoint availability、data freshness 和 source display source 文案；优先通过 domain tests 固化语义，只有实际文案不满足 AC 时再修改生产代码。
- `src/data/projects.ts` 中 Scene Editor 的 tool entrypoint 为 `tbd`，note 为“Scene Editor 仍在开发中，尚未确认公开部署 URL。”；该 entrypoint 与详情、本地仓库和规划文档入口共同构成下一步说明。

### References

- `_bmad-output/planning-artifacts/epics.md#story-33-定义状态含义和内容治理文案`
- `_bmad-output/planning-artifacts/prds/prd-landing-page-2026-05-18/prd.md#fr-25-状态含义文案`
- `_bmad-output/planning-artifacts/prds/prd-landing-page-2026-05-18/prd.md#fr-26-内容新鲜度`
- `src/domain/project-labels.ts`
- `src/data/projects.ts`

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `pnpm typecheck`
- `pnpm test`
- `pnpm lint`
- `pnpm build`
- `pnpm smoke`

### Completion Notes List

- 新增 content governance 测试，固化 status 与 dataFreshness label maps 覆盖范围和文案约束。
- 确认 `in-development` 不表达可启动，manual freshness 明确为人工维护且不表达实时监控或自动同步。
- 覆盖不可用 entrypoint note、label 与 maintainer notes 的短备注边界；当前生产文案无需改动。
- 按 code review 补强中文实时/监控禁词、`disabled` fixture、availability label 语义、不可用入口 UI 邻近渲染和 notes 负面约束。
- 将 Decor Dex 维护备注从 PRD 引用改为发布前复核说明，保持 notes 只承载短维护上下文。

### File List

- `_bmad-output/implementation-artifacts/3-3-定义状态含义和内容治理文案.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `src/components/EntrypointButton.test.tsx`
- `src/data/projects.ts`
- `src/domain/project-governance.test.ts`
- `src/routes/ProjectDetailRoute.test.tsx`

### Change Log

- 2026-05-19: Created Story 3.3 from Epic 3 backlog and moved it to ready-for-dev.
- 2026-05-19: Added content governance tests and moved Story 3.3 to review.
- 2026-05-19: Addressed code-review findings by strengthening governance and UI adjacency tests.
