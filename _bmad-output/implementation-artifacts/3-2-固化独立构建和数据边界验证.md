# Story 3.2: 固化独立构建和数据边界验证

Status: done

## Story

As a Pokopia 工具维护者,
I want 构建和测试证明 Landing Page 不依赖相邻项目内部数据,
so that 目录页不会退化成新的跨项目数据管道。

## Acceptance Criteria

1. Given 开发者运行 `pnpm build`, when 相邻 Pokopia 仓库不存在或没有构建产物, then build 仍能只依赖 landing-page 仓库内容完成, and runtime bundle 只包含项目级公开 metadata。
2. Given 代码库包含 build scripts、tests、UI code 和 data helpers, when 数据边界检查运行, then 不存在对 `../pokopia-color-pattern/dist`、raw source data、图片资产目录、build diagnostics、SceneDocument payload 或 localStorage 数据的扫描、复制或导入, and 任何未来 ingestion 必须通过明确的 Public Project Manifest adapter 决策。
3. Given manifest 数据非法, when 缺少必填字段、enum 拼写错误、重复 id 或 unknown related project id 出现, then validation 在 build/test 阶段失败, and 错误信息可定位到 project id、字段路径和原因。
4. Given Decor Dex 或其他外部 URL 暂时不可访问, when 静态 build 运行, then build 不因外部 URL 不可达而失败, and link checking 如需加入只能作为后续可选检查。
5. Given Public Project Manifest 尚未存在, when MVP 构建和运行, then 页面使用人工维护 Project Manifest 降级展示, and 不执行网络请求、文件系统扫描或通用上游项目爬取。

## Tasks / Subtasks

- [x] Task 1: 增加数据边界测试 (AC: 1, 2, 5)
  - [x] 扫描 source / tests / config，拒绝 active `fetch`、filesystem scanning、localStorage reads 和 adjacent dist imports。
  - [x] 明确允许 manifest 中的 local-only 文本路径和 doesNotRead 文案，不把文案误判成读取行为。
- [x] Task 2: 补足 manifest validation 断言 (AC: 3)
  - [x] 覆盖缺失必填字段、enum 拼写、重复 id、unknown related project id 的定位错误。
  - [x] 确认 `pnpm build` 包含 typecheck/test/build。
- [x] Task 3: 验证并记录 (AC: 1, 4, 5)
  - [x] 运行 `pnpm typecheck`、`pnpm test`、`pnpm lint`、`pnpm build`、`pnpm smoke`。
  - [x] 确认 build 不执行外部 link checking 或网络请求。
  - [x] 在 Vite build 后扫描 `dist`，确认 runtime bundle 不携带相邻项目内部路径或 active localStorage/fs/WebSocket/sendBeacon 读取器。

## Dev Notes

- Landing Page MVP 数据源只能是本仓库人工维护 Project Manifest；不得读取、扫描、复制相邻 Pokopia 项目的 `dist/`、raw assets、SceneDocument payload 或 localStorage 数据。[Source: `_bmad-output/planning-artifacts/architecture.md#data-architecture`]
- Link checking 是后续可选检查，当前 build 不应因外部 URL 不可达失败。[Source: `_bmad-output/planning-artifacts/architecture.md#ci-cd-pipeline`]
- Public Project Manifest 尚未存在时，MVP 使用人工 manifest 降级展示。[Source: `_bmad-output/planning-artifacts/epics.md#story-32-固化独立构建和数据边界验证`]

### References

- `_bmad-output/planning-artifacts/epics.md#story-32-固化独立构建和数据边界验证`
- `_bmad-output/planning-artifacts/architecture.md#data-architecture`
- `_bmad-output/planning-artifacts/architecture.md#critical-implementation-rules`

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `pnpm typecheck`
- `pnpm test`
- `pnpm lint`
- `pnpm build`
- `pnpm smoke`

### Completion Notes List

- 新增 AST + 文本级 data boundary 测试，扫描 `src`、`tests`、`public` 和根配置文件，拒绝 active `fetch`、filesystem scanning/import/require、localStorage access、相邻项目内部 path/import/script copy。
- 测试允许 manifest 中的 local-only 文本路径和 `doesNotRead` 文案，避免把边界说明误判为读取行为。
- build script 已断言为 `tsc -b && vitest run && vite build && vitest run vitest/dist-boundary.test.ts`，并检查 scripts 不包含外部 link checking / URL 命令、相邻项目 copy/sync 或脚本式 ingestion。
- Project Manifest 仍使用 `landing-manifest` + `manual`，未引入 Public Project Manifest adapter、网络请求或上游扫描。
- 边界测试移出 `src`，`tsconfig.app.json` 恢复 browser-scoped types，避免为生产 app 打开 Node globals。
- Vite build 后新增 `dist` 边界测试，禁止构建产物携带相邻项目内部路径或 active storage/filesystem/runtime channel reader token。

### File List

- `_bmad-output/implementation-artifacts/3-2-固化独立构建和数据边界验证.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `vitest/data-boundary.test.ts`
- `tsconfig.app.json`
- `vite.config.ts`
- `package.json`
- `vitest/dist-boundary.test.ts`

### Change Log

- 2026-05-19: Added data boundary enforcement tests and moved Story 3.2 to review.
- 2026-05-19: Addressed code-review findings by moving boundary tests out of app tsconfig scope and expanding JSON/script/dynamic access checks.
