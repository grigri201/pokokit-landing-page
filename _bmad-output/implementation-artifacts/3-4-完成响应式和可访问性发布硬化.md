# Story 3.4: 完成响应式和可访问性发布硬化

Status: done

## Story

As a Pokopia 创作者,
I want Landing Page 在桌面、平板、移动端和辅助技术下都可用,
so that 我可以稳定浏览项目、筛选和进入正确入口。

## Acceptance Criteria

1. Given 用户在 390x844、768x1024、1024x768 和 1440x900 视口打开 Home Page, when 页面渲染项目卡片和筛选控件, then 核心项目信息、status、tags 和 primary Entrypoints 都可见可读, and filter toolbar 换行后不遮挡 Project Card 或造成水平溢出。
2. Given 用户在移动端查看 Project Card 和详情页, when 项目名称、capability tag 或 Entrypoint 文案较长, then 文本正常换行且不重叠, and 触控目标高度不小于 44px 或达到等价可触控面积。
3. Given 用户只使用键盘, when Tab 经过 filters、Project Card links、Entrypoints、related project links 和 recovery actions, then 顺序符合视觉和逻辑路径, and focus ring 可见，Enter/Space 行为符合元素语义。
4. Given 用户使用屏幕阅读器或颜色识别受限, when 状态徽标、disabled/tbd/local-only 入口、空状态和 not-found 状态被读取, then 文本足以理解状态、原因和下一步, and 颜色不是唯一信息来源。
5. Given 发布前 smoke tests 运行, when Playwright 或等价浏览器检查 Home、有效详情、未知详情、filter empty state 和 mobile layout, then smoke tests 通过, and 发现的重叠、不可聚焦或不可读问题必须修复后才能关闭 story。

## Tasks / Subtasks

- [x] Task 1: 增加响应式 Playwright 覆盖 (AC: 1, 2, 5)
  - [x] 覆盖 390x844、768x1024、1024x768、1440x900 首页视口，检查核心信息、status、tags、primary entrypoints 可见。
  - [x] 检查 filter toolbar 不造成水平溢出，Project Card 在换行后仍位于 toolbar 下方。
  - [x] 覆盖移动端详情页、未知路由和 filter empty state。
- [x] Task 2: 增加键盘与可访问语义 smoke (AC: 3, 4, 5)
  - [x] 验证 Tab 顺序从 filters 进入项目入口，focus ring 可见，Enter/Space 能触发 filter button。
  - [x] 验证 disabled/tbd/local-only entrypoint note、not-found recovery 和 empty state 都有可读文本。
- [x] Task 3: 修复发布硬化问题 (AC: 1-4)
  - [x] 确保移动触控目标不小于 44px。
  - [x] 移除 viewport-based font scaling，补齐缺失 design token fallback。
- [x] Task 4: 运行质量门禁并更新记录 (AC: 1-5)
  - [x] 运行 `pnpm typecheck`、`pnpm test`、`pnpm lint`、`pnpm build`、`pnpm smoke`。
  - [x] 更新 Dev Agent Record、File List、Change Log，并将状态推进到 review。

## Dev Notes

- UX-DR31 要求测试覆盖 390x844、768x1024、1024x768、1440x900，并检查长项目名、长 capability tag、多个 entrypoints 和空筛选结果。[Source: `_bmad-output/planning-artifacts/epics.md#ux-design-requirements`]
- UX-DR29/30 要求 Entrypoint Button、Filter Control、Related Project Link 和返回链接支持键盘导航，focus state 可见，状态和入口可用性不能只靠颜色表达。[Source: `_bmad-output/planning-artifacts/ux-design-specification.md#accessibility-strategy`]
- UX-DR10 要求 mobile 触控目标建议不小于 44px，长标签、长项目名和按钮文本可换行，不得挤压 CTA 或造成布局溢出。
- 现有 CSS 已使用 mobile-first media query 和 card grid；本 story 应优先通过 Playwright smoke 固化行为，只修复实际暴露的发布硬化缺口。
- Story 3.2 的 `pnpm build` 已包含 post-build dist boundary test；不要移除或放宽。

### References

- `_bmad-output/planning-artifacts/epics.md#story-34-完成响应式和可访问性发布硬化`
- `_bmad-output/planning-artifacts/ux-design-specification.md#accessibility-strategy`
- `tests/landing-page.spec.ts`
- `src/styles/layout.css`
- `src/styles/tokens.css`

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `pnpm typecheck`
- `pnpm test`
- `pnpm lint`
- `pnpm build`
- `pnpm smoke`

### Completion Notes List

- Playwright smoke 覆盖 Home 的 390x844、768x1024、1024x768、1440x900 响应式视口，并检查无水平溢出、filter toolbar 不遮挡 Project Cards。
- Playwright smoke 覆盖移动详情页、全部目标视口详情页、filter empty state、not-found recovery、真实 Tab 路径、键盘 filter 激活和 focus ring。
- CSS 增加 filter button/back link/related link 44px 触控目标，移除 detail/not-found heading 的 viewport-based font scaling，并补齐 `--color-surface-strong` token。
- ESLint 忽略 Playwright 产物目录，避免 `test-results` / `playwright-report` 影响 lint。
- 按 code review 补强长不可断词压力测试、详情标题换行、文本块重叠检查、触控目标面积检查和 detail/related/recovery Tab 覆盖。

### File List

- `_bmad-output/implementation-artifacts/3-4-完成响应式和可访问性发布硬化.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `eslint.config.js`
- `src/styles/layout.css`
- `src/styles/tokens.css`
- `tests/landing-page.spec.ts`

### Change Log

- 2026-05-19: Created Story 3.4 from Epic 3 backlog and moved it to in-progress.
- 2026-05-19: Added responsive/accessibility smoke coverage and moved Story 3.4 to review.
- 2026-05-19: Addressed code-review findings and moved Story 3.4 to done.
